{
  "name": "workouts",
  "version": "1.0.0",
  "description": "Workouts application with separate client and server",
  "scripts": {
    "start": "npm-run-all --parallel client server",
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "install": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "private": true
}
